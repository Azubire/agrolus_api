const createOrder = async (req, res) => {
  const { id } = req.params;
  //userID - owner of produce
  //distributerId - buyer
  // category id
  //price
  //items
  // payment status -default = unpaid|pendind|paid

  // create order
  // order status
  // order total
};

export default OrderController = { createOrder };
